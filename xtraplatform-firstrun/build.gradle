version = '1.1.2'
//version += '-SNAPSHOT'

dependencies {
    provided project(':xtraplatform-util')
    //provided project(':xtraplatform-runtime')
}

jar {
    manifest { 
        instruction 'Import-Package', 'javax.servlet' 
        instruction 'Import-Package', '*' 
    }
}
