version = '1.1.2'
//version += '-SNAPSHOT'

dependencies {
    provided project(':xtraplatform-kvstore-api')

    provided 'de.interactive_instruments:xtraplatform-util'
}

jar {
    manifest { 
        instruction 'Export-Package'
        instruction 'Private-Package', 
            'de.ii.xsf.configstore.file'
    }
}
