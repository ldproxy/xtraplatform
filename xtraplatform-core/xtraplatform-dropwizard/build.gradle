
dependencies {
    //TODO
    provided group: 'org.osgi', name: 'org.osgi.service.http.whiteboard', version: '1.1.0'

    //TODO: the proxy code was upgraded 2 years ago to support servlet 4.0, but there was no new release since
    // therefore the dependency was commented out and the code copied to our bundle
    //embedded group: 'org.apache.felix', name: 'org.apache.felix.http.proxy', version: '3.0.6'
    provided group: 'org.osgi', name: 'org.osgi.annotation', version: '6.0.0'
}
