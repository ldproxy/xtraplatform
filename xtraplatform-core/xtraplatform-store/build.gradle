
maturity = 'EXPERIMENTAL'

dependencies {
    provided project(':xtraplatform-streams')
    provided project(':xtraplatform-web')

}

moduleInfo {
    requires = [
            'jdk.unsupported',
    ]
}