version = '1.1.2'
//version += '-SNAPSHOT'

dependencies {
}

jar {
    manifest { 
        instruction 'Import-Package', 'javax.servlet' 
        instruction 'Import-Package', '*' 
    }
}
