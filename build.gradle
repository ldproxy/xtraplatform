task checkAll {
    dependsOn gradle.includedBuilds*.task(':checkAll')
}
task bintrayUploadAll {
    dependsOn gradle.includedBuilds*.task(':bintrayUploadAll')
}
task publishToMavenLocalAll {
    dependsOn gradle.includedBuilds*.task(':publishToMavenLocalAll')
}
