version = '1.1.0'

version += '-SNAPSHOT'

dependencies {
	// TODO: shouldn't this be provided for everyone
	// no, this is not a bundle, so maybe it is really compile
    compile group: 'org.apache.felix', name: 'org.apache.felix.main', version: '5.6.8'
    compile 'com.google.guava:guava:23.1-jre'
} 
